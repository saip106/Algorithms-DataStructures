'use strict';

var Graph = require('./Graph'),
    assert = require('assert');

describe('Graph', function () {

    describe('when creating a graph of n vertices', function () {

        it('should create a graph of n vertices', function () {
            var graph = new Graph(2);
            assert.ok(graph);
            assert.
        });


    });

});